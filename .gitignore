node_modules/
dist/
.DS_Store
local/
logs/
coverage/


# Local env (never commit)
.env
.env.*
!.env.example

# ugly-studio state — throwaway local dev/IDE state (dev-server log, session &
# codebase index, sandbox, bundled minio/postgres). None of it is committed.
.ugly-studio/
